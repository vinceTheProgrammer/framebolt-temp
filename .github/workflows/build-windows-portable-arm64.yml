on:
  workflow_call:

jobs:
  build-windows-portable-arm64:
    name: Build Windows Portable (arm64)
    runs-on: windows-11-arm

    needs:
      - build-windows-arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Windows staging
        uses: actions/download-artifact@v8
        with:
          name: framebolt-windows-staging-arm64
          path: dist/windows-staging

      - name: Get version
        shell: pwsh
        run: |
          $Version = cargo metadata `
            --format-version 1 `
            --no-deps |
            ConvertFrom-Json |
            Select-Object -ExpandProperty packages |
            Select-Object -First 1 |
            Select-Object -ExpandProperty version

          "FRAMEBOLT_VERSION=$Version" >> $env:GITHUB_ENV

      - name: Package portable ZIP
        shell: pwsh
        run: |
          ./scripts/windows/package_windows_portable.ps1 -Architecture arm64

      - name: Upload Windows portable artifact
        uses: actions/upload-artifact@v7
        with:
          name: framebolt-windows-portable-arm64
          path: dist/*.zip
          if-no-files-found: error